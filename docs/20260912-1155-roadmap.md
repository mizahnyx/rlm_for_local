# Roadmap — rlm_for_local

**Created:** 2026-09-12 11:55
**Kind:** living document. It is updated in place as items move; the prefix is its
creation date and does not change (see §1).
**Companion:** `AGENTS.md` (repo root) is the working-conventions file; this
document is the plan and the ledger.

Status vocabulary: **done** · **in progress** · **pending** · **owner call** ·
**blocked**.

**State at 2026-09-12 13:14:** the enacting sequence (§5) is complete — items
1–13 done. What remains is the ledger's own residue: **DG10** (worker namespace
isolation, the substantive part of the design's §5.3 work), **VD4** (the load-gate `git`
measurement, host-bound), **VD5** (two models never battery-tested) and the owner
calls **OD1–OD5**. Everything else in §4 is closed, each with a dated record and a
mutation proving its guard.

---

## 1. Naming convention (from 2026-09-12)

Every document created under `docs/` from 2026-09-12 onward is named
`YYYYMMDD-HHmm-<topic>.md`, where the prefix is the document's **creation** date
and time.

- A document is **never renamed** when its content changes. The prefix answers
  "when did this come into being", not "when was it last touched".
- **Point-in-time records are never rewritten.** A correction is a new dated
  document that cites the old one and says which reading it replaces — the rule
  this corpus has followed since 2026-07, now made explicit.
- **Repo-root files are outside the rule** (`README.md`, `AGENTS.md`): they are
  entry points, not dated records.
- **Grandfathered, un-prefixed documents**: `docs/operator-guide.md`,
  `docs/rlm-local-manual.md`, `docs/rlm-kernel-manual.md`,
  `docs/extensibility-guide.md`, `docs/load-test-report.md`,
  `docs/conformance/README.md`, `docs/k4-first-run-report.md`. They predate the
  convention, and renaming them would rewrite links across ~42 documents for no
  informational gain. The list is explicit in `scripts/check_docs.py`
  (`LEGACY_UNPREFIXED`), not implied.
- **Enforced mechanically**: `scripts/check_docs.py` reports any un-prefixed
  document under `docs/` that is not grandfathered, and lints dated documents
  from 2026-09-12 onward (older records are history and stay exempt). The
  linter's own falsifiability is reproducible with
  `scripts/check_docs.py --self-test`, which plants one instance of each of the
  five defect classes and a clean negative control.

---

## 2. Where the project stands

Three cycles are closed; the evidence is in the dated records.

| Cycle | Closed | Evidence | State |
|---|---|---|---|
| K1–K4 kernel (vault, gate, index, memory, GEPA) | 2026-07-30 | `docs/conformance/README.md`, `k4-first-run-report.md` | Closed. Tier-2 organic load testing shelved by the owner. |
| Analysis → remediation (F1–F16, S1–S6 → R1–R25) | 2026-09-10 | `20260910-0730-remediation-validation.md` | Closed; R23 deferred by the plan itself. Two unallocated follow-ups (session secret, remote endpoint) fixed the same day (§8). |
| Model suitability on the router | 2026-09-12 | `20260911-0050-…`, `20260911-1219-…`, `20260911-1359-…`, `20260911-1518-…`, `20260912-0219-…` | Closed as a question: which models can drive the harness, and what the battery actually measures. The ledger below carries what it left open. |

Current scale: the `default` weight profile (P1 10 / P4 20 / P6 20 on the quick
battery), P4 sampled three times. Both changed on 2026-09-11/12 and both are
recorded in the documents above.

---

## 3. Model suitability status (re-scored on the current scale)

VD3. The 2026-09-11 sweep recorded its verdicts on `p1-heavy`; the battery has not
used that scale since. `scripts/rescore_sweep.py` re-scores a recorded sweep from
its stored per-probe scores — no model runs — and reproduces the recorded numbers
when pointed at the old profile, which is what makes the conversion trustworthy:

```
$ python scripts/rescore_sweep.py logs/router-model-battery.jsonl --verify-with p1-heavy
ok: every record is reproduced by p1-heavy
```

| Model | `p1-heavy` | `default` | Verdict (`default`) |
|---|---|---|---|
| Qwen3-4B-2507 | 100.0 | 100.0 | SUITABLE |
| Nanbeige4.2-3B-Heretic | 100.0 | 100.0 | SUITABLE |
| MiniCPM5-2B | 100.0 | 100.0 | SUITABLE |
| Qwen3-VL-4B | 100.0 | 100.0 | SUITABLE |
| Qwen3-VL-4B-Abliterated | 100.0 | 100.0 | SUITABLE |
| Qwen3.5-4B-HauhauCS | 90.0 | 86.7 | SUITABLE |
| Qwen3.5-4B-Q38-Heretic | 90.0 | 86.7 | SUITABLE |
| Qwen3.5-4B-Abliterated *(baseline)* | 90.0 | 86.7 *(derived)* | SUITABLE |
| Huihui-0.8B-Abliterated | 70.0 | 60.0 | MARGINAL |
| Qwen3.5-2B-Instruct | 60.0 | 46.7 | **NOT SUITABLE** (changed) |
| Qwen3.5-0.8B-Unsloth | 50.0 | 33.3 | **NOT SUITABLE** (changed) |

Eight models are SUITABLE on the current scale; two MARGINAL dropped a band. The
baseline row is *derived* rather than swept: it has no record in the sweep JSONL,
and its recorded per-probe profile (P4 full, P6 10/15) is the same one the two
86.7 rows carry, so the same arithmetic applies — labelled derived because it was
not re-read from a file.

Not battery-tested: `Qwen2.5-VL-3B` and `LFM2.5-2.6B-Heretic` (both failed the
one-turn screen; see §3.2 of the router assessment,
`docs/20260911-0050-router-model-assessment.md`).

---

## 4. Open ledger

### Verification debt — claims nobody has measured yet

| ID | Item | Status |
|---|---|---|
| VD1 | **Cache-state hypothesis**: does a warm router really make the flaky model submit? Fits every observation, never isolated. | **done** — controlled comparison: cold 0/2, warm 2/2 on the identical prompt (`20260912-1226-p4-cache-state-confirmed.md`). P4 measures the cold case, which is the operator-relevant one. |
| VD2 | **Trial 3's mechanism**: "did not run" vs "ran but did not take effect" — the trajectory lacks the runtime cell state that would tell them apart. | **done** — the REPL reports `answer_state` at cell end; the diagnostic distinguishes rebound / intact-and-not-reached / harness-inconsistent. It also fixed a pre-existing bug: an unusable `answer['ready']` value used to strand the cell as a timeout. |
| VD3 | All recorded verdicts on a retired scale. | **done** — §3, `scripts/rescore_sweep.py` |
| VD4 | Load-gate phase 5 (`git status` over 100K files) NOT MEASURED. | blocked (this host's ACL broker; needs another filesystem/host) |
| VD5 | Two models never battery-tested. | pending (~1.5 h each) |

### Battery soft spots — scoring validity

| ID | Item | Status |
|---|---|---|
| BS1 | P3 grants 15/15 to a model that never executes anything ("no stderr ⇒ recovery"). | **done** — a model with no cells scores 0; cells that all run clean keep 15/15 with the reason stated. |
| BS2 | P3's recovery scan is not time-ordered: a grep *before* the failing turn counts as recovery. | **done** — recovery is now a *later* cell that ran clean. |
| BS3 | P2 counts an unterminated helper call as valid (`_extract_call_text` → `""`, `_balanced_parens("")` is True). | **done** — a call must be delimited to count. |

### Design gaps — documented but not built

| ID | Item | Status |
|---|---|---|
| DG1 | REPL restricted builtins (`input/eval/exec/compile/globals/locals`) — design §5.3. | **done** — implemented for model code, `RLM_REPL_ALLOW_DYNAMIC=1` to opt out. |
| DG2 | `open` jailed to the task directory — design §5.3. | **retired** — unenforceable while imports are permitted, and the worker's own spill reads must reach outside any task dir. Recorded in the manual §6.1 as retired rather than claimed. |
| DG3 | Memory rlimits on the worker — design §5.3. | **done** (`RLM_REPL_MEMORY_MB` → `RLIMIT_AS`, POSIX only; Windows has no `resource`). |
| DG4 | Scaffold names (`answer`, `context`, helpers) not restored after each cell. | **done** — repaired when unusable, reported in the trajectory. |
| DG5 | No SQLite context store — design §5.7 says "swappable in later". | not planned |
| DG6 | Vault templates introspection-only → `nudges`, `prologue`, `fewshots` are inert, so three of five GEPA targets are no-ops. | not next (feature project) — but **stated where the targets are documented** (kernel manual §9.3 and §11.7) so a run against one is not mistaken for a measurement |
| DG7 | `call_api` bindings pattern documented but absent from code. | **done (already)** — the extensibility guide §8.5 states plainly that no `call_api` binding and no `KernelBridge.handle_api_call` exist, with the four RPC verbs that do; verified while working item 12 |
| DG8 | `Config.max_depth` declared and never read. | **done** — removed rather than wired: the symbol no longer exists in `src/`, and §5 item 9 chose removal over a read that nothing needed. |
| DG9 | `get_helper_summaries()` has no production caller. | **done** — wired into the prompt's helper section, with a cap that its mutation entries exercise. |
| DG10 | **Worker internals are reachable from a cell** through `globals()` (`os`, `socket`, `sys`, `_send`, `_recv`), so model code can bypass the restricted builtins by accident as well as by `import os`. Fix: execute cells in a dedicated namespace holding only the documented scaffold, keeping state across cells. | pending (new, 2026-09-12 — the honest successor to the retired `open` jail) |

### Correctness leftovers

| ID | Item | Status |
|---|---|---|
| CL1 | Search returns deprecated/superseded pages — `Index.fts_search` has no status predicate. | **done** — search is active-only by default; `--status` widens it, and results carry their status. |
| CL2 | R23: frontmatter `schema` → `schema_version` (pydantic shadow); needs dual-parse + migration. | **done** — both keys parse; `rlm-kernel migrate-schema` rewrites pages textually, dry-run by default. |
| CL3 | Worker-internal error strings live in the sandboxed worker, not `templates.py`. | **done** — the worker and the bridge share one set of messages; two mutation entries cover it. |
| CL4 | Slot validation: missing slots are warnings, and a body's `{slots}` are not checked to be a subset of `prompt_vars` — an evolved template could promote and crash `format(**prompt_vars)`. | **done** — unknown slots are refused and every page is slot-checked, shipped templates included; two mutation entries cover it. |
| CL5 | **A mutation entry's verdict was not always deterministic.** `R23 the migration does not rewrite the key` was reported VACUOUS in 2 of 3 full runs (red in every isolated run), and `RO2 the digest starts covering directory timestamps` came back green about 1 run in 4. | **done — both causes found, and neither was the mutation table's logic** (1) *Stale bytecode*: CPython validates a `.pyc` against the source's **size and its mtime truncated to whole seconds**, so a mutation that leaves the file the same length and lands in the same second as an earlier compile is imported from the old bytecode — the R23 entry shortens `schema_version` to `schema` by 7 bytes, exactly as the entry before it does, so in a full run the second reused the first's bytecode. Each mutation run now compiles into its own `PYTHONPYCACHEPREFIX`. (2) *A coin-flip test*: the walk-vs-index digest comparison only *usually* sees a directory-mtime difference, so that mutation's verdict depended on whether two reads caught the same timestamp — replaced as the primary target by `test_a_directory_mtime_change_does_not_move_the_digest` plus its control `test_a_file_mtime_change_moves_the_digest`. Plus a safety net: a green first result is **confirmed once** before it becomes a verdict (`RERUN` is printed; the confirmation's result is recorded). Evidence: both entries went red 5/5 after the fixes, where before they were intermittently green. The runner's own behaviour is covered by `tests/test_guard_nonvacuity.py` (5 tests) rather than by a mutation of itself — a table that mutates its own source mid-run is a new hazard, not a new guard. |
| CL6 | **A search counted the index it was searching.** `corpus_search` and `corpus_coverage` called `TextIndex.coverage()`, which counts `text_chunks` (25M rows), `classification` (4.28M) and `mine_queue` (2.88M). Measured 2026-09-15: `search` 0.2 s, `COUNT(*) FROM text_chunks` **972 s**, the other counts >45 s each — against a 120 s REPL cell limit. A live run died after two hours with `(No answer produced — forced finalization failed)`, its four failing cells being cell timeouts and worker restarts. | **done** — coverage is now a *published* snapshot (`TextIndex.publish_coverage`/`published_coverage`): `mine run` publishes at the end of every window, `rlm corpus counters --refresh` publishes on demand, and the search path only reads; with no snapshot it says **unknown**, never zero, and an old snapshot reports its age. Four new tests include the guard that a miss executes **no** counting query against those tables (sqlite trace callback), 3 mutation entries are red, and the measurements are recorded in `docs/20260915-2305-corpus-search-cannot-count-its-own-index.md`. **Left open**: the refresh is still ~16 minutes, and two candidates are recorded rather than done — run the index in WAL mode, and keep the search connection read-only (`ensure()` currently takes a write lock on every search, which is a second reason a search can stall while mining). **`mine status` no longer counts** — closed 2026-09-22 by RO22, which also found that this leftover was costing a window an hour on its closing path. |

### Owner calls and hardware

| ID | Item | Status |
|---|---|---|
| OD1 | Tier-2 organic 100K load corpus. | **revived by the owner 2026-09-12** — a real read-only corpus now exists. The load report says the Tier-2 gate revives *unchanged* via `RLM_KERNEL_LOAD_CORPUS`; the caveat is that its earlier "insufficient quality and quantity" verdict was about a different corpus, so suitability is unknown until inventoried (§7 RO1). |
| OD2 | K3 vectors / `sqlite-vec`. | owner call (deferred) — becomes load-bearing for corpus retrieval (§7 RO4). **The measurement that makes this a live decision, not a deferred one (2026-09-19)**: over two six-question sets, **four of six questions were served only `none`/`weak` bands** — the index offered nothing that answered them, and a harness cannot cite what it was not given. Every other measured limiter (the cell budget, the encoding, the orientation turns) has now been addressed or instrumented; this one has not, and it is the owner's call. `docs/20260919-2215-the-second-question-set.md`. **Then measured properly (2026-09-20, 13 runs) and split in two**: an **off-question** query — one sharing no content word with the question — was served **`none` 40 times out of 40**, while an **on-question** query was served `strong=36, weak=44, unbanded=1`. So a drifting model cannot be rescued by recall (nothing to find when the question is not in the query), *and* half the on-question evidence is `weak` with `partial` never firing, which is what `OD2` would have to beat. The order that falls out: fix query construction at the prompt first (free), then re-measure whether `weak` still dominates before buying embeddings. `docs/20260920-2104-does-the-model-search-for-the-question.md` | | |
| OD3 | K5 remote vault, media, mounts, UI. | owner call (later phases) — the **mounts** part is now on the critical path (§7 RO2) |
| OD4 | Memory-wiki service spec (`20260723-1324-…`) — a separate product scope, never started. | **promoted by the owner 2026-09-12**: this is now the destination, and §7 is its first stage |
| OD5 | Host `max_instances=4` on a 15 GiB box is an OOM risk; the sweep's `--before-each` restart is a workaround. | owner call (hardware) |
| OD6 | **Root-tier model choice.** The `laptop` profile pins `Qwen3.5-4B-Abliterated` for both tiers: battery **90.0 / 86.7 SUITABLE**, P1 20/20, P4 15/15, **P6 10/15**, 1 848 s. `Nanbeige4.2-3B-Heretic` is **100.0 / 100.0**, P6 15/15, is text-only 3B against a 4B-with-vision (presets both at `ctx-size = 65536`), and took **4 325 s** — the slowest of the five 100-scorers. `MiniCPM5-2B` is also 100/100 in **818 s**, the best capability-per-second on the router. | **owner call, deferred 2026-09-15 (decide later, keep mining)** — the battery evidence and the cost asymmetry are recorded above; what is *not* measured is the part that decides the corpus work (helper use, citation compliance, guard turn cost), because P1/P4/P6 do not touch it. The decision-relevant test would be the graded question on each model, ≥3 runs each (cache-state instability is recorded), scored on cited / grounded / refusals / turns / elapsed against the recorded baseline (0 cited in 3 prompt-only; 1 cited after 2 refusals, 8 turns, 1 574 s with the guard). Switching for a test needs no code change: `--model` overrides both tiers. |
| OD7 | **Task-shaped, personal profiles — and a corpus-task probe suite to fill them in.** The owner's direction (2026-09-16): the router will keep gaining models, so profiles should become personalized and optimized rather than hardware-shaped. The gaps, measured rather than guessed: all three profiles point at the **same** model and differ only in budgets (`tiny` 12 turns / 8 k ctx, `laptop` **15** turns / 16 k, `workstation` 20 / 32 k) — while every corpus run measured here passed `--max-turns 8`, so the laptop default for a corpus question is a ~45-minute run chosen before any convergence evidence existed; `load_config` layers a TOML over the profile but the CLI exposes **no `--config`**, so a personal profile means editing `config.py`; and `assess_router_models.py` probes **protocol** (P1/P4/P6), which would have scored today's fabricating 4-turn run as fine, because its protocol shape was valid. | **recorded, not scheduled — the current order stands** (relevance signal → the escape arm's harness-side evidence → RO7 batch synthesis). The design worth building when it is next, in four parts: (1) **task profiles** (`corpus-ask`, `wiki-synthesis`, `extract-eval`) with their own model, turn budget, cell timeout and temperature, each value traceable to a measurement; (2) a **corpus-task probe suite** beside the protocol sweep — the graded question N runs per model, scored on *cited / grounded (cited ⊆ served) / coverage stated / refusals / turns / elapsed*, all of which now have instruments (`corpus_citation`, `corpus_uncited`, `REPLSandbox.corpus_addresses_served`, turn accounting) and are written by hand into dated records today; (3) **`--config` plumbing plus a `profile write` path**, so a personal profile is data rather than a source edit; (4) the discipline that keeps it honest — ≥3 runs per model (cache-state instability is recorded), host state noted, a one-flag experiment before any default changes, and promotion as the owner's call. Until then, OD6's conclusion holds: switching for a test needs no code change, because `--model` overrides both tiers. |

---

## 5. Enacting sequence

Order matters in two places: VD2's source change precedes VD1's expensive live run
so that run captures the richer evidence, and the cheap mechanical fixes precede
the two judgement calls.

| # | Item | IDs | Status |
|---|---|---|---|
| 1 | Re-score the recorded sweep; commit the tool and this table. | VD3 | **done** |
| 2 | Record the cell-end runtime state in the trajectory so P4 can separate "never ran" from "ran and did not take effect". | VD2 | **done** |
| 3 | Warm/cold controlled P4 test on the router (same query, both states, repeated). | VD1 | **done** — cold 0/2, warm 2/2 |
| 4 | Tighten P3: no free credit for an empty trajectory; time-order the recovery scan. | BS1, BS2 | **done** — `20260912-1211-p2-p3-scoring-tightened.md` |
| 5 | Tighten P2: an unterminated call is not a valid call. | BS3 | **done** — same record |
| 6 | Resolve design §5.3: restore scaffolds per cell, add restricted builtins, add memory rlimits (POSIX), and retire the `open` jail with its reason recorded. | DG1–DG4 | **done** — `20260912-1212-design-53-implemented.md` |
| 7 | R23: `schema` → `schema_version` with dual-parse and a migration path. | CL2 | **done** |
| 8 | Search status predicate, so deprecated/superseded pages stop matching. | CL1 | **done** — `20260912-1256-search-status-predicate.md` |
| 9 | Inert and dead code: wire or remove `max_depth` and `get_helper_summaries()`. | DG8, DG9 | **done** |
| 10 | Move the worker's error strings into `templates.py`. | CL3 | **done** |
| 11 | Slot-subset validation for contract/template promotion. | CL4 | **done** |
| 12 | Docs honesty: mark `call_api` aspirational; state the inert-template gap where the GEPA targets are described. | DG6, DG7 | **done** |
| 13 | Sync living docs, run the full verification set (fast suite, mutations, doc lint + self-test), push. | — | **done** — items 9–12 are recorded in `20260912-1314-ledger-leftovers.md`; 69/69 mutations red, 860 passed, lint clean |

Each item lands as its own commit with its tests, its non-vacuity proof where it
adds a guard, and its dated record when it produces evidence worth keeping.

---

## 6. Explicitly not next

- **DG6 (inert vault templates).** Wiring `nudges`, `prologue` and `fewshots` to
  the vault would make three GEPA targets live. That is a feature, not a defect,
  and it changes runtime behaviour well beyond the harness's own text.
- **VD4 (the 100K `git` measurement).** Not measurable on this host; recording it
  as such is more honest than a number produced on a different machine.
- **OD2, OD3, OD5.** Still owner calls, listed so they are not mistaken for
  forgotten work. (OD1 and OD4 left this list on 2026-09-12 — see §7.)

---

## 7. Corpus phase — "talk to the backup, generate a linked wiki"

**Opened 2026-09-12 by the owner.** The goal: point the harness at a large
read-only personal backup (unorganised text, HTML, source, PDF, archives, music,
video) so the RLM can answer questions against it and grow a linked wiki from it,
with the harness improving itself through RLM/GEPA while it does.

This is OD4's spec (`20260723-1324-rlm-memory-wiki-service-spec.md`) reached from
the harness side rather than built whole: that spec already defines the vault, a
content-addressed store, providers ("mounts") over heterogeneous sources, the
resumable per-item ingestion state machine, bounded LLM enrichment, hybrid
retrieval and the `rlm_local` helper contract (`memory.search`, `wiki.get`, …).
§7 stages it so that something is useful long before the service exists.

### The owner's hard constraint: strictly read-only over the corpus

Recorded as a non-negotiable rule in `AGENTS.md` §1.8, with the three enforcement
layers and the proof obligations. The short version: **the boundary is the mount,
not our code**, because model-authored REPL cells run as the harness user and the
`open` jail was retired as unenforceable (DG2). Everything derived from the corpus
(index, CAS, vault, checkpoints) lives outside it, and a startup assertion refuses
to run if the derived root is inside the corpus root.

### Ledger

| ID | Item | Status |
|---|---|---|
| RO1 | **Inventory the corpus** read-only: file counts by type/size, what fraction yields usable text, what is only media, what is archive-only. Deliverable: a dated inventory record + a decision on what is in scope for text. | **census + Stage 0 done; Stage 1 running** — one read-only pass counted 4,972,609 entries: 4,281,585 files / 1,084,767,249,184 bytes, 649,536 directories, 41,361 symlinks, 98 non-UTF-8 paths. Stage 0 (index-only format census, `docs/20260913-0930-corpus-stage0-format-census.md`) split the bytes by format class and showed the corpus is media-and-machine-state with a ~14 GiB text core. Stage 1 (`rlm corpus classify`, 2026-09-13) reads each file's head and records `kind`, `encoding` and a content hash, closing the 1.56M files / 191 GiB that extensions could not classify. The "what counts as text" decision is now a decision with numbers under it, and is still the owner's. **The composition, measured 2026-09-19 from the classification table**: of 4,972,609 entries — text 2,882,822, binary 724,571, media 489,066, archive 99,993, empty 63,748, document 19,453, database 1,919. Of the *text* files, code is ≈37% (`.js` 670,466, `.py` 159,148, `.c/.h` 129,166, `.java` 110,280, `.php` 5,171, `.sh` 4,924), markup 256,888 and data 114,165, while human prose is **105,816 files (3.7% of text)** and documents 4,933. Recorded encodings: utf-8 2,834,521, **cp1252 46,107**, latin-1 628, none 1,566 — the cp1252/latin-1 set is the one whose accented words the text index cannot currently match. `docs/20260919-1318-owner-findings-first-question-set.md`. |
| RO2 | **Read-only source + `localtree` provider**: a mount protocol with no write verbs, `O_RDONLY` reads, containment and symlink refusal, streaming (never materialising a directory), plus the read-only mount itself and its recorded proof (`mount` line, pre/post manifest, empty `find -newer`). | **done; proof complete for the live session, one obligation left** — mount: `~/Misc/mount-luks-usb-ro.sh` (LUKS `--readonly` + `ro` mount + `/srv/corpus` bind), sysfs/block-layer `ro=1`; code: `rlm_kernel/mounts.py` + 29 tests + 4 mutations. The proof is now harness commands (`rlm corpus verify`, `rlm corpus digest`), and the digest comparison passed: **4,972,609 entries identical between an index built during the session and a fresh 69-minute walk afterwards** (`docs/20260913-0845-corpus-read-only-proof.md`). The marker form of the proof is unusable on this drive — it contains future-dated entries, so "newer than the marker" is true forever; that false breach, and a second one from volatile directory mtimes, are recorded in the same document. **The proof has now been re-run over the whole mining campaign** (2026-09-16): the same two commands, the index's pre-mining rows against a fresh 107-minute walk, and the result is **the same digest as 2026-09-13** — 4,972,609 entries, `8da7068336d3ed8c…`, 1,084,767,249,184 bytes — across three chained 12-hour mining windows, a 29M-chunk text index over 2.88M files, and ten live model runs (`docs/20260916-1130-corpus-read-only-proof-after-mining.md`). **Left**: hashing a *sample of file contents* on top of the manifest — the digest covers path, kind, size and mtime, so a write that preserved size and mtime would be invisible — and the same proof for the pre-index history. |
| RO3 | **Corpus index** (no LLM): extract text for the in-scope formats, index into the kernel's FTS5, so search works over the tree. Reuses the C1 index-backed path — the measured gap between index (48 ms) and tree walk (101 s) at 100K pages is the reason nothing may walk the corpus. | **paths, classification, extraction and the text index all built; the work is now a long queue** — `rlm corpus index` wrote 4,972,609 paths in 95 min; Stage 1 (`rlm corpus classify`) added `kind`/`encoding`/content-hash for all 4,281,585 files in 11.5 h, reading 41.7 GiB of heads (`docs/20260913-2110-corpus-stage1-content-classification.md`). The owner's **mining** shape is implemented (`rlm_kernel/mine.py`, `rlm mine plan/status/run/pause/retry`): a map-driven queue, content-hash-keyed derivation caches, and a windowed worker with a heartbeat lock, pause file, budget and deadline. Measured 2026-09-14: `list_archive` 54,915 done (33 failed, 19,446 with no listing engine, 24,041 pending) recording **10,546,237 archive members**; `extract_text` 5,354 done (3 failed, 517 skipped, all OCR-pending); `index_text` **36,745 done, 11 failed, 2,846,051 pending** at the start of the 2026-09-14 window. A 12-hour `index_text` window was then started (22:40) and measured while it ran: **~60 items/s sustained** (58–66 over 90 s, sampled 2026-09-15), taking the queue to **753,450 done — 26.1% of the 2,882,822 text files** — which puts the remaining ~2.13M at about **10 hours of windows**. That rate is roughly twice the earlier 33 files/s estimate, so the earlier "~24 h" projection was pessimistic. VLM/ASR/OCR/synthesis are queued task names with no handler, and the worker records them as `no_handler` rather than faking them. **The container half is finished (2026-09-22)**: `list_archive` reached **pending 0** — done 98 176 (65 949 fresh, 32 227 cache hits), skipped 123, failed 136 — recording **16 757 795 archive members**, with the terminal skips now a known set (`.rar` 81, `.7z` 39, three singletons) instead of a queue. It took two windows and one ~31-minute deliberate coverage publication once the closing-path defect was fixed (RO22); `docs/20260922-1033-the-container-listing-is-complete.md`. |
| RO4 | **Harness access**: corpus search/get reachable from a REPL cell, so `rlm ask` can answer against the corpus by decomposition. Embeddings (OD2) become load-bearing here for semantic recall. | **done and exercised live; three live runs, one guard added** — `corpus_find`, `corpus_list`, `corpus_stat`, `corpus_read`, `corpus_count`, `corpus_search` and `corpus_coverage` are answered in the parent process through the mount, advertised in the system prompt in both prompt paths, and covered by 195 tests across the corpus modules (`test_corpus.py`, `test_textindex.py`, `test_read_address.py`, `test_corpus_repl.py`, `test_mounts.py`, collected 2026-09-14) and 22 `RO4`-labelled mutation entries. First live question (`Qwen3-4B-2507`, `default`-scale verdict: SUITABLE) answered the corpus's entry/file/directory counts exactly in 904 s. Two later runs against the content index failed informatively: `corpus_search` returned one formatted string, so `len(hits)`/`hits[0]` gave a character count and the letter `A` (fixed: it returns a list, and `corpus_read` takes a printed address); then a run submitted "not mentioned in the corpus" with **zero** helper calls, which is now refused once by `NUDGE_CORPUS_UNSEARCHED` — an evidence-based guard, because the parent serves every helper request (`docs/20260914-2135-corpus-live-runs-and-the-unsearched-nudge.md`). The rerun after that guard (`T4` in that record) searched twice, read three passages and printed seven addresses — the guard did its job — and then submitted 179 characters **with no citation**. On the owner's call (2026-09-14) the answer contract now requires a `Citations:` line of `<path>#L<start>-<end>` addresses, and compliance is **measured rather than enforced**: every accepted corpus answer counts into `RootLoop.corpus_answers`/`corpus_answers_uncited` and writes one `corpus_citation` guardrail event carrying `answers_with_address=True|False`, at all three points where an answer becomes final. **The prompt-only phase has now been measured and it failed**: three consecutive runs of `Qwen3.5-4B-Abliterated` (`laptop` profile, `--max-turns 8`, index at ~34%) searched, read up to five passages, printed addresses — and cited nothing, 0 for 3 (`docs/20260915-0655-corpus-citation-compliance-measured.md`). The owner made the refusal conditional on exactly this result, so the guard is **implemented** (`NUDGE_CORPUS_UNCITED`, `_refuses_uncited`, own nudge budget, applied to both submission channels including a courtesy `FINAL:` line, which also now carries the unsearched rule), with the escape hatch the instruction already names: an answer is accepted if it cites an address **or** reports that the corpus does not hold the answer with its coverage — otherwise a refusal loops on a 34%-indexed corpus, where "nothing citable" is the common case rather than the exception. **Remaining**: whether the guard produces cited answers or merely more turns, which the same `corpus_citation`/`corpus_uncited` events will show on the next live run. — **answered by that next run** (`docs/20260915-1023-corpus-citation-guard-live.md`): the guard fired twice, the model spent its whole 8-turn budget, and the forced-finalization answer came back with a `Citations:` line whose address was one it had actually printed (cited set ⊆ printed set, verified as sets over the trajectory). So the requirement is satisfiable when the harness insists — at the cost of 1574 s and the full budget, an n=1 sample, and an escape hatch that was never exercised live because the question had an answer. Remaining work: exercise the hatch on a question the corpus cannot answer, measure the guard's turn cost across several runs, and the index reaching 100% (42.5% at the time of that record). Coverage is the other honesty problem: 1.27% of the text files when that rerun happened, rising to **26.1% (753,450 of 2,882,822)** after one evening's niced window at ~60 items/s, with `corpus_coverage()` reported alongside every search — so the answer is retrieval-poor until the windows finish, and the number is reported rather than assumed. **The windows finished on 2026-09-16: the text index is complete — 2,881,592 of 2,882,822 sources, 29,015,791 chunks, 41.6 GB, `[coverage: complete]`**, and a search quotes that snapshot in ~1 s instead of counting 25M chunks (roadmap CL6, `docs/20260915-2305-corpus-search-cannot-count-its-own-index.md`).

**The terminal path was the last gap, and it is closed.** On a question the corpus cannot answer, three attempts measured the same failure — the model explores its whole turn budget, never submits, and the answer is delivered by forced finalization, so the citation guard (which sees only submissions) never fired: 0 refusals, 0 citations, twice. `FORCED_FINALIZATION_CORPUS_PROMPT` now asks the terminal answer for its evidence, and the third attempt came back with `answers_with_address=True`, a `Citations:` line, and an address the run had printed (`docs/20260916-1005-corpus-terminal-citation-measured.md`). Two things it did not fix, both measured: the model **cites an adjacent passage rather than saying "not found"**, so the escape arm is still unexercised; and **convergence is unchanged** (8/8 turns, forced, all three attempts). Next by evidence: turn-budget awareness (say what is left, and that "I did not find it" is a complete answer), then the escape arm's harness-side evidence — it is a literal `coverage`-word test today, so a truthful paraphrase is refused. **Convergence, first attempt, measured and insufficient (2026-09-16)**: the turn header already said `Turn 8/8`, so the missing piece looked like permission — `NUDGE_CORPUS_LAST_TURN` now tells a corpus run on its *final* turn (having looked, never submitted) that "I did not find it" is a complete answer, costing no turn of its own. **It fires and changes nothing**: attempt 4 was 8/8 turns, forced, 12 helper calls, 0 citations, and the answer states coverage in prose without ever submitting (`docs/20260916-1405-corpus-convergence-permission-was-not-enough.md`). The run also produced the insight that reframes the problem: a *complete* index makes an unanswerable question search-**hard**, not search-empty — the model searches the question's ordinary words, gets real hits, and has no signal that they are not an answer. Next levers, in the order the evidence supports: (1) make the final turn structurally submission-only rather than advisory; (2) stop paying for exploration we do not use — the forced answer is now the good one (cited in attempt 3, coverage-stating in attempt 4) and takes 8 turns and 25–37 minutes, so a smaller corpus turn budget is a two-run experiment with no code change; (3) give hits a relevance signal, which is also the judgement every wiki page will need. **Lever (2) has now been measured and rejected** (`docs/20260916-1445-corpus-turn-budget-buys-speed-not-provenance.md`): 4-turn and 2-turn runs are 2.5–4× faster (375–669 s against 1 474–2 247 s) and print **no addresses at all** — and one 4-turn run produced a `Citations:` line for an address it was never served, i.e. a **fabricated citation**, which the groundedness check flags (`cited ⊆ printed` false) but nothing refused. **That hole is now shut** (`docs/20260916-1540-corpus-citation-must-be-served.md`): `REPLSandbox.corpus_addresses_served` records every address a search returned and every address a *successful* read resolved (a failed one serves nothing, or asking would legitimise it), `_unserved_citations` diffs the answer against that set, and an answer with a non-member is refused **even when it also names coverage** — the escape arm is for absence, not for a receipt that points at nothing. Refusals log `unserved=N`. **Its live arm has not fired**: two further 4-turn runs of the very question that fabricated emitted **no citation at all**, so nothing unserved reached an answer — 1 fabrication in six short-budget runs, n far too small to call a rate, and the model's instability across router cache states is already recorded (`docs/20260916-1555-corpus-fabrication-not-reproduced.md`). The honest framing: the guard did not fix the model, it made the failure **undeliverable**, which is the right shape for a harness. This is also the groundedness test RO7 needs: an address in a generated page either corresponds to something a helper served or the page is wrong about its own evidence. So the default corpus budget stays at 8, and the next item on the list is the **relevance signal** — a weak match and a strong one still look identical to the model, which is why a short run cannot tell "these are not an answer". Embeddings remain open (OD2). |
**A citation must also answer the question (2026-09-17).** The served-address rule closed fabrication — a citation pointing at nothing — but said nothing about a citation pointing at a passage that does not answer the question, and on the now-complete index that is the remaining failure: the search is *hard*, not empty. The band a search labelled each hit with is now read back at submission (`REPLSandbox.corpus_address_bands`, parsed from the hit's own header line only), the strongest band per address wins, and an answer whose cited addresses were **all** served `weak`/`none` is refused with `NUDGE_CORPUS_WEAK_EVIDENCE` unless it names coverage; one `strong`/`partial` citation is enough, and an address with no band (`corpus_read`, or a stopword-only question) is `unknown` and refuses nothing. Refusals are their own event (`corpus_weak_citation`, with `band=` and `addresses=`), and the prompt states the rule. 10 tests, 6 mutation entries red. **Unverified: no live model has ever been refused by it** — over-refusal is the risk, and the first live run of the graded question is the obligation the record leaves open (`docs/20260917-0410-corpus-a-citation-must-answer-the-question.md`).

| RO5 | **Revive the Tier-2 load gate** (`RLM_KERNEL_LOAD_CORPUS`) against the real corpus: reindex time, search p95, and the git measurement that this host could not produce. Fixes VD4 with a real number. | pending |
| RO6 | **Selective enrichment**: summarise/classify the top-ranked fraction only. The arithmetic decides this, not taste: at the measured 6.6 tok/s prompt / 3.0 tok/s decode, one 500-token summary of a 10 KB document is ~5–10 min, so 100K documents is ~1–1.7 years of continuous inference. The corpus cannot be uniformly summarised on this hardware; it can be *indexed* (no generation) and summarised by value. | pending |
| RO7 | **Wiki synthesis** (the deliverable the owner asked for): cluster by topic, generate source/topic pages, suggest links, all through the gate — with the link suggestion non-destructive to page bodies. Per spec §7.3. | pending |
| RO8 | **Self-improvement loop**: corpus-derived eval suites for GEPA (decomposition contract + few-shots), and LATM-style authored extractors for the format long tail — extracted code goes to quarantine, is validated, and is promoted by review (wiki-service spec §9.4). The heterogenous corpus is what makes authored extractors worth having. | pending |
| RO9 | **Media, later and selectively**: metadata and tags for music/video first (cheap), transcription/captioning only where it earns its cost. `rlm_local` never ingests binary media into a context; media are pages *about* artefacts. | pending |
| RO10 | **Traces a human can assess** (owner call, 2026-09-17): *"some sort of readable by me traces … so I can browse and assess how much the process is suitable to the task, which corpus citations are relevant in some scale."* | **done (v1)** — two halves. **Instrumentation**: every `corpus_*` call now writes a `corpus_served` event carrying the addresses it handed over *with the band each came with* (`ok=false` when it failed), because the counters could not answer "was this citation served, and did the passage behind it answer the question?" **Viewer**: `rlm trace render` (one Markdown page per run + `index.md`) and `rlm trace summary` (counts, writes nothing). A page carries the question, the outcome, the counts, a five-bucket **citation audit** (cited-and-answering / cited-but-non-answering / cited-but-never-served / cited-but-unknown / served-and-never-cited) and **the passage behind every cited or served address**, resolved read-only through the mount. Privacy is structural: the pages contain corpus text, so `--out-dir` is refused inside the corpus root and written 0600 in a 0700 directory, and only the path and counts are printed (`render_summary` carries no question, address or quote). An audit says **partial** when the trajectory cannot support it (no `corpus_served` events → the cited addresses land in `cited_unknown`, never in `cited_unserved`; or a torn last line). 30 tests, 9 mutation entries, suite 1295/0, `docs/20260917-0915-corpus-traces-a-human-can-audit.md`. **Open**: the owner has not read a page yet — the viewer's value is unproven until they judge it — and pages rendered for pre-instrumentation runs have partial audits by construction. |
| RO11 | **`corpus_read(<address>)` scanned the whole chunk table** (found 2026-09-17 by running the viewer on the real corpus). `find_chunk` filtered on `text_chunks.display`, which has no index, so one passage read scanned 29 015 791 rows: measured **43.14 s** for a search and **>150 s (no return)** for one read against a 120 s cell limit — a run could search but not open what it found, which is how a citation is supposed to be checked. | **file-backed addresses done, one residual open.** The lookup now resolves display → exact bytes through the path index (`entries_path`, one indexed lookup) and filters on the indexed `source` column: the same address reads in **3.86 s** (measured on the complete index), with trace-based tests (asserting the `WHERE` clause is on `source`, never `display`) and 2 mutation entries. **Open**: a container member (`arch.zip!member.txt`) names no file on disk, so it still takes the display fallback and over 150 s; the trace viewer refuses to read those and marks them on the page, and the corpus helper keeps the slow-but-correct path. The fix candidates, in cost order: index `text_chunks(display)` (a multi-GB index build on the owner's disk, and a write to the derived index), or resolve members through the derivation cache. **Owner call on the index build**; the measurement is `docs/20260917-1040-corpus-a-read-that-scanned-every-chunk.md`. |
| RO12 | **Two failures that were not the model's** (owner's review, 2026-09-17): *"Sometimes the model fails to produce valid Python, those steps must be repeated without penalty until valid Python is generated"* and *"60 seconds timeout for `corpus_count` is really too little, that parameter has to be configured somehow… a methodology must be devised probably to diagnose when a harness failure is product of a too low value"*. | **done.** A `SyntaxError` is now a *formatting* failure: the turn counter stands still (the loop became an explicit `while` for this), the error budget is untouched, and the retry is bounded by a new `max_syntax_retries` (5) with `syntax_retry`/`syntax_giveup` events. `cell_timeout` is configurable per invocation (`--cell-timeout`, `RLM_CELL_TIMEOUT`), and a timeout is now a first-class, diagnosable event: `cell_timeout` carries `budget=` and `last_helper=`, the model is told it hit a *budget* rather than a bug, and `RootLoop.cell_timeouts` counts them. The diagnosis methodology is written into the operator guide ("Was it the budget?", §3). 7 tests, 6 mutation entries. **Not verified live**: no run has yet emitted `syntax_retry` or `cell_timeout`, so the fix's cost (extra calls inside a turn) and `corpus_count`'s real budget under load are unmeasured — `docs/20260917-1200-two-failures-that-were-not-the-models.md`. |
| RO13 | **Mnemonic addresses and structured hits** (owner's review, 2026-09-17): *"the addresses returned by `corpus_search` … must be biunivocally translated back and forth into some mnemonic form … highly repairable"*, and *"corpus search hits are being interpreted sometimes character by character"*. | **done and unit-verified (2026-09-21); a live model has not yet cited through an alias.** `src/rlm_local/mnemonics.py` holds the pure core (per-session `AliasTable`, `resolve` with the five-step order, `substitute_addresses`), each search hit is a `HitRecord` with named fields and a teaching integer index, `corpus_read` takes an alias or an address, the citation audit maps a cited alias back before the served-set check, the delivered answer carries true addresses while the trajectory keeps the model's raw output, and the trace page shows alias beside address. The **parent owns the table**, answering the worker-versus-parent question the handoff left open: the worker never learns the vocabulary. **Three design claims were corrected by measurement** (`docs/20260921-0118-mnemonic-aliases-ownership-and-three-corrections.md`): the check symbol is a corruption detector and **cannot** be injective over 93 750 codes; the repair step must **not** filter candidates by check symbol, because narrowing the set can leave one survivor — the filtered rule resolved **11 of 3 110** sampled slips to the wrong passage and the uniqueness rule resolved none; and the body grew from three glyphs to four, because at 3 750 codes a sampled session produced a wrong-passage resolution while at **93 750** it does not. Evidence: **1 444 passed / 8 skipped / 12 deselected / 0 failed** on the fast suite, and **13 RO13 mutation entries all red** — four were rewritten after the table reported them vacuous, and one was deleted because the guard it targeted is unreachable. **Left open**: a live run of the prompt-experiment question on the same index, to turn `citation_repaired` from an instrument into a number; the baseline to beat is that question's `IndexError=3, ValueError=1`. **designed, not implemented — owner gate.** The design: a per-run alias table (`KQ7-3`-style tokens from a confusable-free alphabet with a check symbol), bijective within a run and never reused across runs, with a five-step `resolve()` that repairs case/confusable/edit-distance-1 corruptions **only on a unique winner** and otherwise refuses with the candidates named; every repair is a `citation_repaired` event. Hits become records with named fields whose integer indexing raises a naming error instead of returning `'S'`. Literature, the owner gates and the deciding measurement are in `docs/20260917-1215-mnemonic-addresses-design.md`. **Two gates settled by the owner (2026-09-17)**: aliases are **per chat session** (spanning many runs; an `ask` run is a session of length one), and **the harness sees mnemonics while the owner sees true addresses, transparently** — the trajectory keeps the model's raw output, the mapping is on the trace page, and every repair is an event. **Four decisions still open**, each with the reasoning laid out in `docs/20260917-1330-decision-brief-mnemonics-budgets-and-proofs.md`. **Two more settled by the owner (2026-09-17)**: the harness **substitutes the true address inline** in the delivered answer (the model's untouched output is kept in the trajectory and the substitution is logged), and **structured hits land before the aliases**. **Two remain**, and the owner asked for them explained from the ground up before deciding: what to do about RO11's archive-member residual, and whether `corpus_count` gets a published snapshot plus an operation budget — `docs/20260917-1400-two-decisions-explained-from-the-ground-up.md`. Writing the test for the classification below also **refined the repair rule**: an adjacent transposition must count as one edit (Damerau, not plain Levenshtein), because swapping two neighbouring characters is the classic slip on a short token and a rule that refused it would fail on the corruption it exists for. **The data gate is now open (2026-09-19)**: the owner, reading the pages of the first real question set, reports the model trying to handle direct addresses and getting stuck in the process — the evidence this item was waiting for. Structured hits first, then the aliases, as decided; no live run has yet produced a cited answer that needed a repair. `docs/20260919-1318-owner-findings-first-question-set.md`. **Owner-called to start (2026-09-20)**, on a sharper finding: reading the prompt experiment's page, the owner identified the three `IndexError`s as the model *"trying to 'massage' the address part of the results of `corpus_search` in order to feed one of them to `corpus_read`"* — string surgery on `path#L<start>-<end>` — and concluded *"a strong case to start using mnemonics instead of plain addresses."* That supersedes the earlier ordering: the alias table lands first, and each hit becomes a record that **carries** its alias, so the record half arrives with it rather than before it. The rest of the 2026-09-17 decisions stand (per-session aliases, harness sees mnemonics, owner sees addresses, inline substitution with the raw kept, every repair an event). Implementation-ready plan, with the pieces, tests and mutations: `docs/20260920-2305-mnemonics-the-owners-call-and-the-plan.md`. Not built. |
| RO14 | **A container is a directory, and a cache miss is a work item** (owner, 2026-09-17): *"compressed files should behave transparently as directories … the correct way is either route thru the extraction cache, or if that can't be done, it probably signals that the compressed file must be marked as needed to be mined."* | **done and unit-verified (2026-09-21).** `corpus_read` resolves a `container!member` reference before the address lookup: the container comes from the indexed path column, its extraction from the derivation cache (key derived through `DerivationCache.key`, the call mining used to write it), and the reply names the substitution because the text is the container's, not the member's. A container with no cached extraction answers `CORPUS_CONTAINER_NEEDS_MINING`, which names the mining operation and offers &quot;the corpus does not contain this&quot; as a truthful answer. No scan either way. **The expensive input was not the one the record assumed**: a *bare* member name never reached the chunk lookup at all (no `#L` fragment, so the mount check answered &quot;no such path&quot; — unreadable rather than slow), while the **address** form `…!member#L0-9` took `find_chunk`'s `display` fallback: 29 015 791 rows, over 150 s against a 120 s cell limit, finding nothing. The guards are therefore trace-based — a member read must issue no statement filtering `text_chunks` on `display`. 17 tests, 4 mutation entries, all red; two of them first came back VACUOUS and each found something real (a miss-path test that used the bare form, and a fixture with no `text_chunks` table to scan), and one `entries` guard was deleted because the container check already covered it. **Not verified against the live corpus** — the 150 s is the earlier measurement of the removed path, and a live member read belongs with the RO13 live run. `docs/20260921-0550-a-container-member-reads-through-the-cache.md`. **decided, not implemented.** `arch.zip!member.txt` reads through the extraction cache like any path; when the cache cannot answer, the read path says so and the container goes onto the work list instead of falling back to a scan of 29M rows. This **replaces RO11's residual** — no `text_chunks(display)` index, which also disposes of a multi-GB index build on the owner's disk. 30 tests for structured hits to come with RO13; the read-path change is small and is the first half of RO15. |
| RO15 | **The cache freshness ledger** (owner, 2026-09-17): *"Both questions have to do with the up to date state of different speed up caches against the corpus. We will probably need to add some form of 'to do list' of operations that would make all the caches actual with respect to the corpus. That will also be handy when we have more dynamic mounts like journals or personal notebooks."* | **the first piece is built (2026-09-21): the fingerprints, and a read-only report.** `rlm_kernel/freshness.py` plus `rlm corpus freshness` answer the design's four questions per artefact — what it is derived from, whether it is **current** (its inputs have not moved, which is not the same as being recent), the operation that would make it current, and how much work that is. Two rules keep it honest, both from this project's history: a cache with **no fingerprint reports `unknown`, never `current`** (AGENTS.md §1.8 with a new subject), and **the diagnosis is never the expensive operation** — every marker compared is an indexed `COUNT(*)` over `mine_queue` or a value already in the coverage snapshot, with trace-based tests asserting the ledger issues no statement touching `text_chunks` or `archive_members` (counting the 29M-row table here would be CL6 again). An artefact with no implemented check also reports `unknown`, so a spec added without a branch cannot be silently current. Fingerprints are recorded where a cache is known current — the end of every mining window and `corpus counters --refresh`, both through `publish_coverage_snapshot`. 11 tests, 2 mutations red. **Deliberately not in it**: the path index and the classification rows, whose honest staleness test is a walk or a re-read of every head — they join when the writer has a generation counter, rather than being answered by a scan. **Still open from the design**: whether a *search* should caveat or refuse when the text index is behind (the design leans caveat for search, refusal only for `unknown`), and the ledger's two later reading surfaces (the trace index page and the console) — the CLI first was the owner's call and is what landed. **designed, not implemented.** `docs/20260917-1510-cache-freshness-ledger-design.md`: for every derived artefact (path index, classification, text index, coverage snapshot, counts snapshot, archive listings, extraction cache) — what it is derived from, whether it is **current** (input markers still match, not "how old"), the operation that would make it current, and how much work that is. A cache miss is a work item; a cache with no recorded fingerprint reports **unknown**, never current. The measurements that settled its cost unit are in `docs/20260917-1530-what-a-corpus-call-costs.md`: **storage bytes read**, not seconds and not engine steps — two of the worst queries (231 s and 637 s) reported ~0M engine steps because they were waiting on I/O, so an operation budget would guard the cheap work and sleep through the expensive work. First pieces: input fingerprints + a read-only report; the archive cache-miss path (RO14); a write-time running byte total so `SUM(size)`'s 3.43 GB becomes free. **Settled by the owner (2026-09-17)**: the report is read on the **CLI first**, then the trace index page, then the console; a search over a *known-stale* index **caveats** and over an *unknown* one **refuses**; and — the gate the implementation ran into — **an index that carries no fingerprint at all refuses until one exists** ("run `rlm corpus counters --refresh`"), rather than being grandfathered as current, because grandfathering is the confident-default this project ranks below silence. The first fingerprint is written by that refresh and by the end of every mining window. Remedies are typed `queue` or `command` in one table. |


| RO16 | **A cell's budget: bytes, and two configurable time limits** (owner, 2026-09-17). The measured basis: the same `COUNT(*)` is 231 s cold and 0.69 s warm; `SUM(size)` reads 3.43 GB; two of the worst operations did ~0M engine steps; and all three live `cell_timeout` events landed on `corpus_search`. | **the two time limits are done; the bytes unit is not.** (1) **Bytes read** — as decided and still unbuilt: a property of the request given the cache state, with each helper reporting what it spent, and a watchdog thread (polling the process's I/O counters, calling `sqlite3.interrupt()`) rather than a progress handler, which cannot see a query waiting on I/O. (2) **Two time limits per cell, both configurable: landed 2026-09-18.** A *soft* limit (`cell_timeout`, 60 s / 60 s / 120 s by profile) **signals**: a cell that has asked the harness for anything is demonstrably working and is granted the *hard* limit (`cell_timeout_hard`, default 1 200 s = 20 min), announced on an operator warning sink **while the cell runs** and recorded as a `cell_extended` event; a cell that asked for nothing is stuck and is stopped at the soft limit. **No run-level ceiling** — the owner's call, recorded so it is not re-litigated. `--cell-timeout` / `--cell-timeout-hard` and `RLM_CELL_TIMEOUT` / `RLM_CELL_TIMEOUT_HARD`, over the profile defaults; equal values switch the second stage off. The `cell_timeout` event now carries `limit=soft|hard`, `budget=`, `activity=` and `last_helper=`, and the model is told the budget that actually fired. 5 sandbox-level tests, 3 integration/CLI tests, 7 mutation entries, all red. **The first attempt had been reverted (2026-09-17)** because its two-stage tests observed no helper activity (`corpus_calls=0` when the extension branch was live, `last_helper=corpus_coverage` when it was not); the sandbox-level strategy that replaced root-loop timing found **two real defects in the reverted code** — the extension branch sat where a real wait never returns and so could never fire, and the first "deadline reached" test compared the clock, which a socket timeout beat in about one run in four — `docs/20260918-0451-two-stage-cell-budget-landed.md` (design: `docs/20260917-2042-decisions-recorded-budget-and-ledger.md`; the failed attempt: `docs/20260917-2059-blocked-two-stage-budget-and-a-gate.md`). **Measured live at the real limits (2026-09-18, `scripts/probe_cell_budget.py` on `lunacode`, soft 60 s / hard 1 200 s, `two_limits_supported=True`)**: the same cell body with a helper call is extended at 60 s (`cell_extended … last_helper=corpus_coverage`) and **runs to completion in 75.00 s with `timed_out=False` and 0 timeouts**; without one it is stopped at 60.03 s with `limit=soft activity=0` and 0 warnings. Before this change both were killed at 60 s and both read as timeouts. **Still open**: the hard limit has never fired against a real cell (the case that outlives 1 200 s costs ~22 minutes and is unrun), and no *model* run has produced an extension yet — these cells are scripted. `docs/20260918-0510-probe-two-limits-at-the-real-limits.md`. |

| RO17 | **The owner's evaluation loop: sample passages, ask real questions, read the pages** (owner, 2026-09-18): *"the probes only tested the timeouts, in a superficial way. We need real questions that produce more meaningful traces. And also maybe a simple tool for me that outputs random text blocks with their addresses from the corpus, so I can devise meaningful questions for you to use as probes."* | **both tools landed; no real question has been asked yet.** `rlm corpus sample` hands back passages with the addresses that re-read them — drawn by probing a random chunk `id`, so a draw costs a handful of indexed lookups however large the index is, and `--seed` makes it reproducible; vendored paths and container-member chunks (the >150 s family, RO11) are out unless asked for, and the header states that the output is corpus text. `scripts/run_question_probe.py` runs a question set through the harness against the read-only corpus and prints **aggregates only** — the same `render_summary` line as `rlm trace summary`, never an answer, a passage or an address, which stay in one trajectory per question beside the corpus; a question that fails is recorded on its own line and the set continues; the probe has no wall-clock ceiling of its own, by the same owner call that gives the harness none. The question set that may live in the repository asks about aggregates only (`DEFAULT_QUESTIONS`); a set devised from passages lives in `~/rlm-derived/questions/`. 21 tests, 7 mutation entries, mutation table 191/0 — and one **vacuous test of my own** caught and fixed by the table (a single-draw duplicate check that could not see a removed dedupe). **Next**: the owner draws passages, writes questions, and the first live set is run and rendered; until then the roadmap's own question — can this harness find, read and cite things in a 4.28M-file corpus? — still has no evidence under it beyond the three earlier live runs. `docs/20260918-0750-sampling-passages-and-probing-with-real-questions.md`. — **the first live set has now run (2026-09-19)**: six owner-written questions, `Qwen3.5-4B-Abliterated` on `laptop`, `--max-turns 6`, soft 90 s / hard 1 200 s, ≈2h08m of wall clock, six pages the owner has read. Findings, with numbers: **no cell was stopped by a time limit, and one cell was extended** (the RO16 mechanism's first live appearance with a real model); **five of six runs ended `forced=True`** (the answer came from forced finalization, which by design bypasses the citation guard); **only one run cited answering evidence**, one served eight `strong` hits and cited none, one served 24 addresses and cited none, one searched nothing at all; **2–5 of the 5–6 turn budget is spent before the model first calls a helper** (first helper turn 3, 3, 3, 4, 4, 6); and the escape hatch fired **live, once** (`AGENTS.md` §2's open item). **A second set was then run on the repaired index** (2026-09-19, same limits): 2 of 6 questions produced an answering citation before and 2 of 6 after — question 2 gained an exact one, question 3 held its two — total wall clock unchanged at ~2h09m, and **four of six questions were served only `none`/`weak` bands**. So the levers the columns point at are retrieval quality (OD2) and the orientation cost, not the encoding. `docs/20260919-1318-owner-findings-first-question-set.md`, `docs/20260919-2215-the-second-question-set.md`. |

| RO18 | **Representation before reading: render, then describe** (owner, 2026-09-19): *"MIME types matter, a lot, some nominally textual files like .obj or .svg could be better understood by 'translating' them first to an image rendering or representation and using either the multimodal capabilities of the model, or an ancillary lightweight vision model to describe them."* | **new, from the owner's read of the first question set; owner call needed on tooling and the model.** The measured population: `.svg` 17,439, `.obj` 1,739, `.stl` 1,348, `.dxf` 44 — **20,570 files that are textual by extension and spatial by meaning** — beside 489,066 media and 724,571 binary already excluded from text. The shape is a new pair of mining task names (`render_preview`, then `describe_image`), not a change to the text path: a renderer per format (a rasteriser for SVG, a projection for 3-D meshes) writing to the derivation cache, then a description from a vision model — the model's own multimodal capability or a small dedicated one. Two things must be settled first: which renderers exist on this host without writing to the corpus, and whether a vision model is available at all (OD5 territory). `docs/20260919-1318-owner-findings-first-question-set.md`. |
| RO19 | **The text index cannot read the encodings it recorded** (found 2026-09-19 by the owner reading the first question set): *"some files have Windows Latin codepoint and thus words with tilded vowels are not found on files with encoding mismatch."* | **fixed and repaired (2026-09-19).** The decode now uses the recorded encoding in the tokenizer and in the read path (`decode_text`), `text_chunks` carries the codec it was decoded with (a live index migrates in place), addresses are untouched because chunk ranges still come from the raw bytes, and `rlm corpus reindex-encodings` repaired what was already indexed: **considered 46 735, re-indexed 46 725, failed 7, in 33m17s**. Measured after: exact token counts rose everywhere, and a deterministic 17-file sample verified word_present=17, encoding_recorded=17. The 7 failures are RO20. `classification.encoding` records it for every file (cp1252 46,107, latin-1 628 of 2,882,822 text files), but `add_text` indexes `text[start:end].decode("utf-8", "replace")` and `TextIndex.read` uses the same call, so those bytes become U+FFFD and an accented word is indexed as fragments — the FTS5 tokenizer's `remove_diacritics` never sees it, because the damage is one step earlier. The fix is to decode with the recorded encoding in both the index and the read path, then re-index the 46,735 affected files (bounded, and cheap beside a 2.88M-file pass). Testable without a model: index cp1252 bytes containing an accented word, search it, read the passage back. **Until it lands, "no matches" for an accented word is not evidence of absence** — which is exactly how question 1 of the first set was answered. `docs/20260919-1318-owner-findings-first-question-set.md`. — **fixed and repaired (2026-09-19).** The decode now uses the recorded encoding in the tokenizer and in the read path (`decode_text`), `text_chunks` carries the codec it was decoded with (a live index migrates in place), addresses are untouched because chunk ranges still come from the raw bytes, and `rlm corpus reindex-encodings` repaired what was already indexed: **considered 46,735, re-indexed 46,725, failed 7, in 33m17s**. Measured after: exact token counts rose everywhere (`después` 9,537 → 25,285, `información` 23,785 → 36,914, `canción` 1,752 → 2,467) and a deterministic 17-file sample verified **word_present=17, encoding_recorded=17** — every sampled legacy file findable by a word taken from its own content. The 7 failures are RO20; the 13-minute estimate in the first version of the help text was 2.5× optimistic and is corrected. `docs/20260919-1708-the-encoding-repair-measured.md`. |


| RO20 | **A path whose name is not UTF-8 cannot be indexed at all** (found 2026-09-19 by running the RO19 repair on the live index): the command died six seconds in with `UnicodeEncodeError: 'utf-8' codec can't encode character '\udced' … surrogates not allowed`. The mount decodes a path with `surrogateescape` (correct — the bytes are what they are), and `text_chunks.display` is TEXT, so the 98 non-UTF-8 paths the census counted cannot be stored: those files are unsearchable, and every one of them also aborts any pass that does not catch it. | **done on the live index (2026-09-21): the 11 text files are indexed and `index_text` has zero failures corpus-wide.** The owner's call was to index them all; measuring first showed that meant **11 of the 98** — none was pending, and grouping by classification gave text 11 (`.js`/`.log`/`.htm`/`.csv`/`.ini`, all `index_text/failed UnicodeEncodeError`), media 27, binary 23, archive 28 (`list_archive/skipped no_listing_engine`), document 1, unclassified 8. The other 87 are media, binary, archives and unnamed files, which a text index has no content for. After (a), `mine retry --task index_text` plus one bounded window took the population from 0 indexed / 11 failed to **11 indexed / 0 failed**, with no `index_text` failure anywhere in the queue. **A sibling of the same defect is left open**: three `list_archive/failed/UnicodeEncodeError` rows (`.gz`/`.tgz`) — another place a raw name reaches a TEXT column, not the chunk display this fixed, recorded rather than chased. **(a) landed and verified 2026-09-21; (b) is your call and the tool for it exists.** `mine._index_display` renders a chunk's display the way `CorpusIndex` already stores a path (exact bytes in `raw`, surrogate-free text in `path`), so `text_chunks.display` holds the string `entries.path` holds and `raw_for` keeps resolving it — chosen over `backslashreplace` because a display the path index does not recognise resolves to `None` and the read path would answer "no such path" for a file that is right there. Both writers now record an `UnicodeEncodeError`/`sqlite3.Error` as a per-item FAILED outcome instead of ending the pass, so one unstorable item is counted rather than truncating a queue (RO19's repair pass learned the same lesson). 12 tests, 3 mutations red. `scripts/sample_encoding_wall.py` produces the input for (b): aggregates only on stdout, output refused inside the corpus root, samples written 0600/0700 beside the corpus. **Its first reading: 98 paths, 113 558 467 bytes, of which 97% is 29 `.mp3` + 1 `.mpg`** — text-plausible **50 files / 358 137 bytes**, media **48 files / 113 200 330 bytes**, nothing between; the only genuine unknown is **38 extensionless files (108 707 bytes)**, and the samples at `~/rlm-derived/encoding-wall/samples.jsonl` are there to settle them. The call: index them all (the collision is the price — two files can present one address), index only the text-plausible ones, or leave them out and record why. `docs/20260921-1336-ro20-a-landed-and-the-sampling-tool.md`. 

| RO21 | **A search re-reads every hit's text, and the index is 38 GB** (found 2026-09-21 by investigating why the RO13 live run's cells died inside `corpus_read`). `handle_search` labels each hit with `covers n/m … (band)` and prints a 300-character snippet. The FTS table is contentless by design, so both come from `text_index.read(...)` — **a real read of the file for every hit, inside the search call**. | **implemented, and the A/B refutes the hypothesis it was built on (2026-09-21).** `TextIndex.read(..., max_bytes=…)` now caps the read and `handle_search` passes `SEARCH_SNIPPET_BYTES` (4 KiB, generous for a 300-character snippet in any encoding here); 6 tests and 2 mutations, both red. Then the before/after, alternating the two checkouts in one driver so the page cache treated them alike: warm-cache the two variants are **indistinguishable** (0.06 s against 0.06 s on the word whose cap actually bites, 8 000 → 4 096 bytes), and round 1's apparent win was cache warming between the variants, not the change. **So read amplification by bytes is not what makes a search expensive**: 43 601 bytes of reads took 1.18 s cold and 0.06 s warm. **The dominant variable is page-cache warmth — 0.02 s warm against 101.24 s cold for the same query, same variant, same host** — which a 38 GB index and ~6 GB of cache cannot avoid. The cap is kept because it is a cap (tested, no behaviour change, fewer bytes) but it is **not** a performance fix and does not remove the cold read. What ranks ahead of it now: make the index fit or be advised (WAL, `mmap_size`, a warm pass before a run), or store the snippet's opening at index time so a search need not open the file at all — at the cost of the "the words live once" invariant. None of those is a one-line change. The one thing still unreproduced is a served address refused with `no such path`. 

| RO22 | **A finished window looked hung because it was counting** (found 2026-09-22, while asking why a container-listing chain had stopped advancing). Window 1 of the chain listed 7 437 containers inside its 25-minute budget, then spent ~51 minutes in `publish_coverage_snapshot`'s whole-index scan and a further hour in `format_status(store.status())`'s `COUNT(*) FROM archive_members` — the CL6 defect twice over, once per window, and neither phase bounded by the budget because the budget is checked between items. The lock had already been **released** before the second count, and the log looked torn because stdout to a file is block-buffered and only the `flush=True` progress lines had landed. | **done (2026-09-22).** `run_queue(coverage_scan=…)` and `mine run --no-coverage-scan` let a chain pay the ~16-minute scan **once**, at the end, instead of once per window; `status()` quotes a *published* member count (`meta['archive_members_published']`) and otherwise says **unknown** with its remedy, which closes CL6's "Left open: `mine status` still counts"; `_pulse` heartbeats the lock from *inside* the closing scan via `sqlite3.set_progress_handler`; and the CLI's closing report moved inside the lock. **One measured constraint shaped it**: a bare `SELECT COUNT(*)` steps **no** VM instructions (0 pulses against 600 for `COUNT(x)`/`SUM(x)` over 200 000 rows, `scripts/probe_count_forms.py`), so a progress-handler heartbeat cannot cover it — `MEMBER_COUNT_SQL` counts a column instead, exact because the column is `NOT NULL`. 7 guards, 7 mutation entries red, and the table caught a vacuous test of mine (a cleanup assertion the 30 s rate limit made blind). **Left open**: the coverage scan is not cheaper, only less frequent; `find_members` still scans the member table; and the lock-takeover hazard is inferred from the code path, never observed. **Verified live the same day** — window 1 ran `--no-coverage-scan` and honoured its budget (1 502.0 s), then closed in **97 seconds** where the pre-fix phase took ~76 minutes, exited `rc=0`, and window 2 started immediately; the closing status line printed `archive members recorded: unknown` with its remedy instead of counting. `docs/20260922-0855-the-window-that-counted-instead-of-finishing.md`, `docs/20260922-0946-the-closing-phase-measured-live.md`. |

### Sequencing, and why this order
1. **RO1 first, because scope is unknown.** The type mix decides everything
   downstream — how much is text at all, how much is inside archives, and how much
   is media that only has metadata worth having. An inventory is read-only by
   construction and costs no model time.
2. **RO2 before RO3.** Indexing a tree we cannot yet prove we never write to would
   be exactly the mistake the constraint exists to prevent.
3. **RO3 before RO4/RO6/RO7.** Retrieval without generation is the cheap half and
   makes the corpus *usable* immediately (search + read), whereas generation is
   the expensive half and needs budget discipline.
4. **RO5 as soon as RO3 lands** — it converts "the index should scale" into a
   measured envelope on real data, and it is the gate the project already defined.
5. **RO8 last, because self-improvement needs something to improve against**:
   eval suites derived from the corpus, not synthetic ones.


### Not decided yet (needs the owner)

- **What counts as in scope for text** (archives expanded or indexed as blobs?
  PDFs OCR'd or metadata-only? source code summarised or only searchable?). RO1's
  census is the input to this call.
- **Whether the wiki service is built per its spec or reached harness-first** —
  this section assumes harness-first, with the service as the destination.
- **The X-scraper project as a pattern source** (the owner mentioned it as
  inspiration): its resume/cursor/idempotency discipline is what the wiki-service
  spec already prescribes for the job queue (its §7.4), so it is worth reading
  before the queue is written — path needed.

### Decided

- **Where the corpus is**: an external LUKS-encrypted drive on `lunacode`, exposed
  read-only at `/srv/corpus` (the `backup/` subtree only). Confirmed 2026-09-12.
- **Where derived state lives** (owner, 2026-09-12): **outside the corpus, on a
  separate root** — for now on the internal disk; if more privacy is wanted later
  it moves to another LUKS drive or partition. Two consequences recorded so they
  are not rediscovered: it cannot live on the external drive while that drive is
  mounted read-only, and it must not be a second mapping of the *same* LUKS
  container (Linux shares one superblock per filesystem, which is exactly the
  non-read-only hazard the mount exists to avoid). The vault root is a single
  path setting, so the later move is a path change rather than a migration.
- **The read-only guarantee is the mount** (`AGENTS.md` §1.8): `cryptsetup open
  --readonly` → sysfs `ro=1` → `ro` mount on both paths → corpus not writable by
  the harness user. Application code is defence in depth, never the boundary.
- **The harness runs beside the corpus** (owner, 2026-09-12): `~/Misc/rlm_for_local`
  on `lunacode`, venv on Python 3.14, derived state at `~/rlm-derived` (0700,
  outside the corpus). The model router is on the same host, so a corpus excerpt
  reaches only `127.0.0.1:9010` and never leaves the machine holding the corpus
  (`AGENTS.md` §1.9). The checkout is kept in sync from the repository rather than
  edited in place.
- **The corpus is reached only through the harness** (owner, 2026-09-12): ad-hoc
  shell work over the tree stops. Reads happen through `rlm_kernel/mounts.py`
  (via `rlm corpus …` or a `corpus_*` helper in a cell), and the index is built by
  the harness rather than by a hand-run walk. The one exception is the mount
  state itself (`findmnt`, sysfs), which is not corpus content.
- **The first index covers names, not contents** (owner, 2026-09-12): paths,
  sizes, mtimes and kinds for everything, no file reads, no LLM. Text extraction
  comes after RO1's scope call.
- **Index windows chain until the text index is complete, then stop** (owner,
  2026-09-15). The owner's machine, so the policy is theirs: a supervisor on
  `lunacode` (`~/rlm-derived/mine-index-chain.sh`) starts a fresh 12-hour
  `index_text` window whenever the previous one ends, stops when the queue has no
  pending items, and stops **for good** the moment the pause file exists
  (`~/rlm-derived/PAUSE-INDEX`) — a supervisor that restarts itself behind the
  owner's back would be the opposite of handing the machine over. It decides
  liveness from the heartbeat lock's age, never by probing a process (`AGENTS.md`
  §3). The remaining ~1.58M text files are ~7.5 hours of windows at the measured
  ~60 items/s.
